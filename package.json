{
  "name": "gemini",
  "version": "1.0.0",
  "description": "Geminijs",
  "main": "gemini.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wrangler pages deploy functions",
    "dev": "wrangler pages dev functions"
  },
  "author": "syed ahamed",
  "license": "ISC",
  "dependencies": {
    "@cfworker/json-schema": "^1.12.8",
    "@cloudflare/wrangler": "^1.21.0"
  }
}
